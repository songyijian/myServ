<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1,maximum-scale=1,user-scalable=no">
    <title>仓库项目</title>
	<link rel="stylesheet" href="/public/css/style.css">
	<link rel="stylesheet" href="/public/js/jquery.js">
</head>
<body>
	<div class="panel">
		
		<% if(err){ %>
			<%= err %>
		<% }else{ %>
			<% for(var j=0; j<data.length; j++){%>
				<% if(data[j].indexOf('.')>0){ %>
					<a href="<%= data[j] %>" target="_blank" class="textfile" ><%= data[j] %></a>
				<% }else{ %>
					<a href="<%= data[j] %>" class="catalog"><%= data[j] %></a>
				<% } %>
			<% } %>
		<% } %>
	</div>

	<a href="/" id="gotoindex">首页</a>

	<script>
	</script>
</body>
</html>
